# Cosmic-Hand-Control
A browser-based 3D interactive app that uses hand gestures to manipulate floating 3D blocks (select, grab, move, rotate, explode, duplicate) with visual particle effects and post-processing.

Run locally:

1. Install dependencies: `npm install`
2. Run: `npm run dev`
